import React, { Component } from "react";

const RenderAuthentication = ({prop}) => {
    
}


const Authentication = (props) => {
  //     if (!props.auth.isLogged) {
  //   }

  return (
    <React.Fragment>
      <RenderAuthentication />
    </React.Fragment>
  );
};

export default Authentication;
